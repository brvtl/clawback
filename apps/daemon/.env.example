# Clawback Daemon Configuration

# Server
PORT=3000
HOST=0.0.0.0
LOG_LEVEL=info

# Database
DATABASE_URL=./clawback.db

# Skills
SKILLS_DIR=./skills

# Claude Backend: "api" | "sdk" | "auto"
# - "api": Use Anthropic API directly (requires ANTHROPIC_API_KEY)
# - "sdk": Use Claude Code SDK (no API key needed, uses local claude)
# - "auto": Try SDK first, fall back to API if SDK fails (default)
CLAUDE_BACKEND=auto

# Anthropic API Key (required if CLAUDE_BACKEND=api, optional for auto)
# ANTHROPIC_API_KEY=sk-ant-...
