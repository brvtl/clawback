# Optional config overrides for the skill
# These values override the frontmatter in SKILL.md

# Add additional knowledge files
knowledge:
  - ./coding-guidelines.md

# Customize notifications
notifications:
  onComplete: true
  onError: true
